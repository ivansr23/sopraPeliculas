server:
  port: 8080
spring:
  application:
    name: mi-config-peliculas
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://mysql:3306/mysql
    username: root
    password: root
  jpa:
    hibernate:
       ddl-auto: update
eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka